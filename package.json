{
  "name": "golden-boot-cup-admin-api",
  "version": "1.0.0",
  "private": true,
  "description": "Golden Boot Cup mini program backend API",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  }
}
